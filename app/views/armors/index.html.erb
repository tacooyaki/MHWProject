<!DOCTYPE html>
<html lang="en">
<head>
  <title>Monster Hunter World Database</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.9.3/css/bulma.min.css">
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body>
<section class="section">
  <p class="title is-3 is-spaced has-text-white">Armor</p>

  <div class="block">
    <%= form_tag armors_path, method: :get do %>
      <%= label_tag :search, "Search by armor name  :" %>
      <%= text_field_tag :search, params[:search] %> |

      <%= label_tag :type, "Filter by armor type:" %>
      <%= select_tag :type, options_for_select(@types, params[:type]) %>

      <%= submit_tag "Filter" %>
    <% end %>
  </div>

  <div class="block">
    <ul>
      <% @armors.each do |armor| %>
        <li>
          <b><%= link_to armor.name, armor_path(armor) %></b>
        </li>
      <% end %>
    </ul>
  </div>

  <%= paginate @armors, class: 'pagination', outer_window: 5, inner_window: 2 %>
</section>
</body>